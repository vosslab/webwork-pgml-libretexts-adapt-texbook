DOCUMENT();
loadMacros(
	"PGstandard.pl",
	"PGML.pl",
);
TEXT(beginproblem());

$choices = RadioButtons(
	["A", "B", "C"],
	"B",
);

BEGIN_PGML
Select B: [@ $choices->buttons() @]
END_PGML

ENDDOCUMENT();
